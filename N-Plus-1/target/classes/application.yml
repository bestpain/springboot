spring:
  application:
    name: N-Plus-1

  datasource:
    url: jdbc:mysql://localhost:3306/60_days_challenge
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
#    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mvc:
    servlet:
      path: /api/
